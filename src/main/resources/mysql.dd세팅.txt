axios import


db - 쿼리 = 코드
jdbc
-> sbjdbc
-> orm(jpa) // 초급단계는 쉬운데 중급없이 상급
-> mapper -> mybatis을 사용하기 위해

inteli j -> porm.xml -> 꺽새 사용 (<) dependency 뜨면 사용해서 많은 라이브러리 사용
mvn에서 많은 라이브러리의 dependency를 사용 할 수 있다.

inteli j -> mvn
<!-- https://mvnrepository.com/artifact/org.mybatis.spring.boot/mybatis-spring-boot-starter -->
<dependency>
    <groupId>org.mybatis.spring.boot</groupId>
    <artifactId>mybatis-spring-boot-starter</artifactId>
    <version>3.0.5</version>
</dependency>
의존 xml 사용

객체의 필드가 주어졌을 때
mysql에 테이블을 만들어 데이터타입과 check 설정

mapper 인터페이스 만들고 @Mapper 달기
insert 받아올 객체 생성하고 매개변수 생성

xml
st설정
<mapper namespace=주소설정>
<insert>

controlle
mybatis/설정
insert설정